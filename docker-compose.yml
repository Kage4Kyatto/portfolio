services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app

  node:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install; npm run start"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
